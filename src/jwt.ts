// import { sing } from './jwt';
// import jwt from 'jsonwebtoken';

// const secret = key;

// export const sing = payloand => jwt.sing(payloand, secret);
//export const verify = token => jwt.verify(token, secret);